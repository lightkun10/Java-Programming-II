package L3_Adding_values_to_the_list;

public class AddingValuesToList {
    public static void main(String[] args) {
        /*
        Now we can add values to the list — or at least we can create
        a list and call the add method. We cannot test if the values
        are actually saved to the list yet.
        */
        List<String> myList = new List<>();
        myList.add("hello");
        myList.add("world");
    }
}
